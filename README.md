# mblue-comm

`mblue-comm` is a shared .NET common library for MBlue projects.  
The project provides the base structure for initializing
and running the virtual world side of communication.

Target framework: `net10.0`
